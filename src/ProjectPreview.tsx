/** Source-derived previews, not simulated product screens. */
export default function ProjectPreview({id}:{id:string}){
 if(id==='drax')return <div className="work-preview preview-llm"><span className="eyebrow">DOCUMENT QUESTION ANSWERING / IMPLEMENTED BEHAVIOR</span><h4>One document.<br/><i>Two ways to ask.</i></h4><dl className="llm-modes"><div><dt>Textbook tutor</dt><dd>Retrieved passages inform the prompt; missing documents trigger an explicit fallback.</dd></div><div><dt>General tutor</dt><dd>Instructions allow broader model knowledge alongside textbook context.</dd></div></dl><span className="preview-caption">PDF retrieval · eight context chunks · saved conversations<br/>Source-based overview, not a live model demonstration.</span></div>;
 if(id==='emotion')return <div className="work-preview preview-study"><span className="eyebrow">TRANSFER LEARNING / REPORTED ACCURACY</span><div className="preview-bars">{[['VGG16',97.85],['DenseNet121',96.04],['ResNet50',85.57],['MobileNetV2',74.74]].map(([label,n])=><div key={label}><span>{label}</span><i><b style={{width:n+'%'}}/></i><strong>{n}%</strong></div>)}</div><span className="preview-caption">Two classes. Four architectures. Evaluation caveats inside.</span></div>;
 const times=[170,190,197,205,220,221,222,230,239,250,255,259,265,270,272,280,281,285,290,295,305,307];
 return <div className="work-preview preview-flight"><span className="eyebrow">DISTRIBUTED DATA / PUBLISHED 12-NODE RUN</span><div className="preview-timing"><strong>170 → 307</strong><span>seconds</span></div><div className="scaling-bars" role="img" aria-label="Reported processing time increases from 170 seconds for one year of data to 307 seconds for 22 years">{times.map((n,i)=><i key={i} style={{height:n/307*100+'%'}}/>)}</div><div className="preview-axis"><span>1 year of records</span><span>22 years</span></div><span className="preview-caption">Transcribed from the project report; not a fresh benchmark.</span></div>
}
